apiVersion: v1
kind: Service
metadata:
  labels:
    app: todoapp-frontend
  name: todoapp-frontend
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    service: todoapp-frontend
  type: LoadBalancer